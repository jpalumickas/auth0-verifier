---
sudo: false
bundler_args: --without development

language: ruby

cache: bundler

rvm:
  - 2.2.0
  - 2.3.0
  - 2.4.0
  - 2.5.0
  - 2.6.0
  - ruby-head

before_install:
  - gem install bundler -v 2.0.1

script:
  - bundle exec rspec

matrix:
  allow_failures:
    - rvm: ruby-head
